<nav class="navbar navbar-inverse navbar-fixed-top navbar-custom">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="navbar-brand">
        <img src="/images/logo/logo_nav.png"/>
      </div>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="<%= @navbar.eql?("Home") ? 'active' : '' %>">
          <%= link_to "HOME", root_path %>
        </li>
        <li class="<%= @navbar.eql?("Suche") ? 'active' : '' %>">
          <%= link_to "SUCHE", search_path %>
        </li>
        <li class="<%= @navbar.eql?("Liste") ? 'active' : '' %>">
          <%= link_to "LISTE", games_path %>
        </li>
      </ul>


      <ul class="nav navbar-nav pull-right ">
        <% if user_signed_in? %>
            <% if !current_user.profile.nil? && !current_user.profile.new_record? %>
                <li class="dropdown">
                  <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                    <%= image_tag current_user.profile.image.url(:icon), :class => "img-rounded" %>
                    &nbsp;<%= current_user.profile.username %>
                  </a>
                  <ul class="dropdown-menu dropdown-menu-right" role="menu">
                    <li><%= link_to 'Kontoeinstellungen', edit_user_registration_path %></li>
                    <li><%= link_to 'Profil bearbeiten', edit_profile_path(current_user.profile) %></li>
                    <li role="separator" class="divider"></li>
                    <li><%= link_to 'Abmelden', destroy_user_session_path, method: :delete %></li>
                  </ul>
                </li>
            <% else %>
                <li><%= link_to 'Abmelden', destroy_user_session_path, method: :delete %></li>
            <% end %>
        <% else %>
            <li><%= link_to 'LOGIN', new_user_session_path %></li>
            <li><%= link_to 'REGISTRIEREN', new_user_registration_path %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>