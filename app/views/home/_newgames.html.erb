<div class="list-group">
  <% @newgames.each do |game | %>
      <li style="height: 40px;" class="list-group-item">
        <div class="cut-word pull-left">
          <%= link_to game.title, game %>
        </div>
        <div class="pull-right">
          <%= l game.releasedate, format: :default %>
        </div>
      </li>
  <% end %>
</div>